# Bugs to report upstream

Reports written against protean's dependencies, ready to file, kept here until
someone files them.

They live in the repo rather than in a chat log because that is the difference
between a queue and a memory. `docs/backlog.md` item 40 recorded that "there are
already two unfiled Mol\* drafts waiting on whose account files them" — and when
this directory was created on 2026-08-28 to hold a third, **neither of those two
was anywhere in the repository.** They were written in a session, described in a
backlog note, and lost. Anything worth filing is worth committing first.

## What is here

| file | project | status |
|---|---|---|
| `molstar-ssao-background-test.md` | Mol\* | **written, not filed** |

## What is missing

The two drafts item 40 refers to. They are not in git history under any name I
could find, so they will have to be rewritten from whatever notes survive, or
abandoned. Recorded here so the count in `docs/backlog.md` is not read as
"three reports exist somewhere".

## Filing

These are written to be pasted into an issue tracker as-is, so each one states
its own reproduction and evidence rather than assuming protean's context. The
open question is **whose account files them**, which is why they are queued
rather than filed. Once one is filed, put the issue URL at the top of its file
and leave the file here — a filed report is still the best record of what was
measured.
